@model IEnumerable<FundRaising.Models.Student>

@using GridMvc.Html
@{
    ViewBag.Title = "Participants";
    Layout = "~/Views/School/_SchoolLayout.cshtml";
}

<h2>Students /Participants</h2>

@*<button type="button" class="btn btn-primary">Download</button>
<br />
<br />
    <input type="text" name="search"  id="txtSearch" placeholder="Search" /><input type="button"  id="btnSearch" value="Search" /><button type="button" class="btn btn-default">Clear Filter</button>
<br />
<br />*@

<div>

@Html.Grid(Model).Columns(columns =>
{
    columns.Add()
                .Encoded(false)
                .Sanitized(false)
                .SetWidth(30)
                .Css("hidden-xs") //hide on phones
                .RenderValueAs(c =>
                @<div id="balloon" class="balloonClass">
                    <b>
                        @*@Html.ActionLink("Edit", "Edit", new { id = c.ID }, new { @class = "modal_link", @Id = "editlink" })*@
                        @*<a href="#" onclick="goToCreate('/School/StudentDetails?id=@c.ID')" class="modal_link" id="editlink" data-toggle="tooltip" data-placement="bottom" title="Edit"><span class="glyphicon glyphicon-envelope space" aria-hidden="true"></span></a>*@
                   
                      <a href='/School/StudentDetails?id=@c.StudentID'><span class="glyphicon glyphicon-envelope"></span></a>  
                    
                    </b>
                </div>);

    columns.Add()
                .Encoded(false)
                .Sanitized(false)
                .SetWidth(30)
                .Css("hidden-xs") //hide on phones
                .RenderValueAs(c =>
                @<div id="balloon" class="balloonClass">
                    <b>
                        @*@Html.ActionLink("Edit", "Edit", new { id = c.ID }, new { @class = "modal_link", @Id = "editlink" })*@
                        @*<a href="#" onclick="goToCreate('/School/StudentDetails?id=@c.ID')" class="modal_link" id="editlink" data-toggle="tooltip" data-placement="bottom" title="Edit"><span class="glyphicon glyphicon-envelope space" aria-hidden="true"></span></a>*@

                        <a href='/School/OrderDetails?id=@c.StudentID'><span class="glyphicon glyphicon-usd"></span></a>

                    </b>
                </div>);

    columns.Add(c => c.StudentID).Titled("Student ID");
    columns.Add(c => c.FullName).Titled("Student Name");
    columns.Add(c => c.EmailAddress).Titled("Student Email");
    columns.Add(c => c.TeacherName).Titled("Teacher Name");
    columns.Add(c => c.DateRegistered).Titled("Date Registered");

}).WithPaging(10).Sortable().Filterable().WithMultipleFilters()

</div>

<script>
    //$(document).on('click', '#btnSearch', function () {

    //    var data = $('#txtSearch').val();


    //});

</script>
<script src="~/Scripts/Common.js"></script>